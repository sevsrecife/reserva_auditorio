{
  "name": "reserva-auditorio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply RESERVAS_DB --local",
    "d1:migrate:remote": "wrangler d1 migrations apply RESERVAS_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.29.0"
  }
}
